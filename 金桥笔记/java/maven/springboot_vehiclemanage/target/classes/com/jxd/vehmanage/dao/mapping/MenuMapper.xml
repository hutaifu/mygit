<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jxd.vehmanage.dao.MenuMapper">
<!--         Map<String,String> selectMenu(@Param("mno") int mno);-->
<select id="selectMenu" resultType="map">
    select mno,one,two,tfirst,tsecond from menu where mno = #{mno}
</select>
</mapper>